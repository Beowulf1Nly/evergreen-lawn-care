# Keep large, UNUSED originals in the repo but out of the published site so
# GitHub Pages publishes fast/reliably. Nothing on the live site references these.
# (Files remain in the repo — this only controls what gets served.)
exclude:
  - Photos          # ~47MB of unreferenced original photos; site uses hero.png/og-image.jpg
